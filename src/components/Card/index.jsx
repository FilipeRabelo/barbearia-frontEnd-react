// import { barbeiros_falsos } from "../../mocks/barbeiros";

import { DivStyle } from "./style";

export function Card({children}) {
  return (    
    <DivStyle>
      {children}
    </DivStyle>
  )
}